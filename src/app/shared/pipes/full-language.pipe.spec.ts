import { FullLanguagePipe } from './full-language.pipe';

describe('FullLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new FullLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
